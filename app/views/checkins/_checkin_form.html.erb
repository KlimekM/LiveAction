<% if flash[:error] %>
  <%= flash[:error] %>
<% end %>

<%= form_for [@place, @checkin] do |f| %>
  <%= f.label :description %>:</br>
  <%= f.text_area :description %></br>

  <%= f.date_select :date_attended, order: [:month, :day, :year] %></br>

  <%= f.submit %>
<% end %>